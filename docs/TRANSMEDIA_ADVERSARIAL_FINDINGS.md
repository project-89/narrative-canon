# Transmedia Adversarial Findings — 2026-07-20 sweep

**Status**: `reference` — the confirmed findings behind `TRANSMEDIA_INTEGRATION_REVIEW.md` §10.
**Provenance**: 172-agent adversarial workflow — 7 critique lenses (Opus) over the
integration plan + codebase; every finding faced 3 independent refutation votes
(Sonnet) against the actual code; 53 raised → **40 confirmed** (13 critical /
23 major / 4 minor), 13 refuted. Proposals below are the verifier-refined
versions where a refinement survived.

Findings are the WHY behind §10's architecture. Check items off as the phases
land; a finding is closed only by behavior-verified code, not by plan text.

## [CRITICAL] durability-1 — Non-atomic 54MB blob write; T0b dual-write can only desync

**Claim**: The write path corrupts or desyncs the entire world on any mid-write interruption, and T0b's plan to append a typed op-log 'alongside the blob write' makes this strictly worse because two separate file writes cannot be made atomic.

**Evidence**: saveProjectData (src/api/server.ts:294-311) does `fs.writeFileSync(projectDataFile, JSON.stringify(data, null, 2))` directly — no temp-file + atomic rename, no fsync, no lock. Its own comment (299-303) admits tsx-watch restarts have killed writes mid-flight. A 54MB blob exists (.narrative-data/project_project_1770455263375.json). A crash during a 54MB writeFileSync truncates the ONLY copy — losing all films/entities/commits, not just the last change. TRANSMEDIA_INTEGRATION_REVIEW.md:172-176 …

**Fix**: Do the cheap, uncontroversial fix first and don't over-scope the rest: (1) Before T0a lands, replace the direct writeFileSync in saveProjectData with write-to-`<file>.tmp` + fsync + `fs.renameSync` over the target (atomic on the same filesystem) — fixes the truncation/corruption failure mode with a ~10-line change and no architecture shift. (2) For T0b, don't yet promote the op-log to "source of truth with blob as rebuildable cache" — that effectively pulls forward T4's content-addressed-commit architecture into T0, a bigger bet than the spine phase needs. Instead just fix the ordering/atomicity of the two writes: append the operation to the log first (itself durable via O_APPEND + fsync, safe/atomic for appends without rename tricks), and only then perform the atomic blob rewrite from (1). That gives a cheap, clear recovery invariant for T0b's lifetime: worst case after a crash is …

## [CRITICAL] scale-1 — Every mutation rewrites the whole 54MB blob synchronously; op-log adds amplification

**Claim**: The 'blob = cheap materialized view' assumption is false: each of 126 save sites serializes and synchronously writes the ENTIRE world, blocking the event loop; adding an emitOperation on every tool call compounds this rather than reducing it, and a nightly autonomous run turns into hundreds of full-blob rewrites.

**Evidence**: saveProjectData (server.ts:307) JSON.stringifies and writeFileSyncs the full ProjectData every call, on the request thread. Real blobs are 54MB, 28MB, 25MB (.narrative-data). Existing `commits` already embed a full `snapshot` (verified: 13MB for 2 commits in project_1770455263375). T0b keeps this AND appends an op per tool call (REVIEW.md:172-176); the blob is still rewritten in full each op, so write volume goes UP. T0a (154-158) makes project = world with productions[], so a 10-film world is …

**Fix**: The original proposal (append-only op-log as the sole hot write path, blob materialized lazily/debounced; per-production file sharding; side-files for large arrays) is directionally right but should be sequenced and scoped more carefully given what's ratified: 1. Kill the double-write immediately in T0b: emitOperation() must not be additive to the existing sync writeFileSync of the whole blob. Either (a) make the op-log append (O(op), fsync'd for durability) the primary synchronous write, with the full blob write moved to a debounced/background flush (e.g. every N ops or T seconds, or on process-idle/shutdown), or (b) if a synchronous durability guarantee is still wanted per-request, only sync-write the op-log entry (small), never the full blob, and rebuild/refresh the materialized blob asynchronously. 2. Stop embedding full snapshots in commits. The 13MB-for-2-commits data point shows …

## [CRITICAL] missing-1 — No atomic/concurrency-safe persistence under new autonomous writers

**Claim**: The plan (T0b emitOperation alongside the blob write; T3 hooks firing nightly dream_film + compose_comic + distributor agents; T6 many character-agents) introduces multiple concurrent, unattended writers, but the entire persistence layer is a synchronous full-blob rewrite of a single shared mutable object with no locking, no atomic rename, and no backup — so the plan as designed will silently lose updates and can corrupt whole projects.

**Evidence**: server.ts:294 saveProjectData does projectDataCache.set then fs.writeFileSync(projectDataFile, JSON.stringify(data)) directly onto the live file (no tmp+rename) — a crash mid-write truncates the entire project. All ~130 mutating handlers share one in-memory ProjectData via projectDataCache (server.ts:296) and there is no lock/queue (grep for lock/mutex/writeQueue in save path: none). Commit 'snapshots' deep-copy entities+interactions+storyGraph INTO the same blob (server.ts:8578-8580), so blobs …

**Fix**: Before T0b/T3: (a) make saveProjectData write to a temp file + fs.renameSync (atomic on same fs); (b) add a per-project async write lock/serialized queue so concurrent handlers and hook-triggered pipelines can't interleave read-modify-write; (c) stop deep-copying full snapshots into the blob (store commit snapshots out-of-line or as diffs) so a growing world doesn't produce 50MB+ rewrites per mutation; (d) add a rolling backup (copy prior blob) so a corrupt write is recoverable. Treat as a T0 prerequisite, not deferred to T4.

## [CRITICAL] product-1 — Canon rail (the one new surface) has no persistent data substrate

**Claim**: The plan makes the Canon rail the hub's centerpiece — 'the world's inbox: incoming branches grouped by source' with merge/bounce/fork actions and morning reports landing there (§8), and says 'even a v1 listing operation-log batches with merge/bounce changes how the system feels.' But there is nothing durable to list. Proposals live on `session.pendingProposals`, an in-memory field reset to `[]` on every session load (server.ts:9435) and NOT persisted by saveConversationHistory, which writes only messages/worldContext/currentFocus/userDecisions (server.ts:9450-9456). They surface only inline in the chat turn's response payload (server.ts:20282). Morning reports land in the scratchpad, not an …

**Evidence**: server.ts:9435 `pendingProposals: []`; server.ts:9450-9456 saveConversationHistory omits pendingProposals; server.ts:20195 'no auto-accept, no silent changes'; server.ts:15313 report lands in scratchpad; review doc lines 43,112-118 confirm commits/branches are 'fake VCS' with branch:'main' hardcoded.

**Fix**: Keep the core proposal but sharpen the target: the missing piece isn't just "persist pendingProposals" — it's that T0b's `emitOperation()` operation log (as specced) records ops *at the moment of the blob write* (i.e., already-merged/canon changes), which is a history log, not a pre-merge review queue. The Canon rail needs a genuinely separate durable structure: persisted, non-canon "incoming" commits (keyed by source: chat proposal, ingest job, dream job, external `commit_event`) that sit in a pending/accepted/bounced/forked state on a real branch (not `main`) until a human or the autonomy dial merges them. Concretely: 1. Make T0b's scope explicit: emitOperation() must be able to append to a *non-main branch* (not just log alongside the main blob write), and the studio's proposal-accept endpoint (server.ts ~20543) must be changed to create a branch commit rather than writing directly …

## [CRITICAL] schema-1 — Op vocabulary covers ~6 of ~20 ProjectData collections; blob-as-view is false

**Claim**: T0b ("route the executor switch through emitOperation()… the blob stays the materialized view", review §4/§7) cannot hold: the v1 op union only expresses entity/relationship/scene/frame/style/scratchpad mutations, so the op log can never reconstruct — and would DELETE on any rebuild — most of what the 130 studio tools produce.

**Evidence**: GraphOperationSchema (schemas.ts:335-385) has exactly 16 members, all over Entity/Relationship/Scene/Frame/StyleProfile/Scratchpad. ProjectData (storage-adapter.ts:196-224) also carries timeline (tracks/items — the whole Production editing line, :151-194), acts, script (beatSheet/sceneList/write :95-134), assets, artifacts, generatedImages, conversationHistory, plus per-scene runtime with NO op type: scene.explorations (server.ts:13154), frame.videoTakes (server.ts:6434), scene.productionRun …

**Fix**: Mostly agree with the finding's option (a)/(b) framing, with one adjustment: don't scope the full ~2x vocabulary expansion into T0 — that's a lot of schema surface to build before any consumer (hooks/merge) exists to read it, and T4 is already "full nit" content-addressed commits, which is the natural point to demand full coverage. Instead: 1. Immediately fix the doc's false claims: strike "the blob stays the materialized view" and "under T0b every action already emits typed operations" from §4/§7. Replace with an explicit statement that T0b ships a PARTIAL op journal (entity/relationship/scene/frame/style/scratchpad only) and the blob remains authoritative/non-reconstructible-from-ops through at least T3. 2. Ship T0b as scoped now (entities/relationships/scenes/frames/style/scratchpad) since that's exactly what T1 (comics) and T3's first hooks (portrait-on-entity-add, nightly render …

## [CRITICAL] schema-2 — v1 has no container above one Narrative; productions[]/shared world can't fit

**Claim**: The ratified canonical schema (review §6.1) tops out at a single Narrative with one flat scenes[] and one entities[], and RepoManifest is scoped to one narrativeId — yet T0a makes the project a WORLD holding productions[] (each its own script+timeline+scenes) with entities/looks shared across them (§4 T0a, §8). The canonical schema physically cannot hold the plan's highest-leverage change.

**Evidence**: NarrativeSchema (schemas.ts:314-329) = { entities[], relationships[], scenes[], styleProfile?, scratchpad? } — entities and scenes are CO-LOCATED in one document; no productions[], no world node. RepoManifestSchema (schemas.ts:447-465) has singular narrativeId + one head, but §7 asserts 'the world is a repository'. The migrator hard-codes the single-scene-list assumption: raw.interactions → one scenes[] (migrate-from-studio.ts:354). Narrative→production duplicates shared entities per production …

**Fix**: Restructure v1 BEFORE T0a: add a World/Campaign container (worldId, shared entities/relationships/styleProfile/looks) holding productions[], each Production owning { format, scenes[], timeline, exports, style override }; make RepoManifest world-scoped with productions as sub-refs. Rewrite migrate-from-studio.ts to emit World{ productions:[production0] } instead of a bare Narrative. Treat this as the real T0a deliverable — 'just pluralize ProjectData.productions[]' understates it because the ratified schema, not just the blob, must change.

## [CRITICAL] schema-3 — Content hash strips extensions.studio.*; Zod strips unknowns — lossless data unaddressed

**Claim**: Two load-bearing properties collide: 'lossless — unknown fields preserved under extensions.studio.*' (§1B, migrator) vs 'content-addressed commits' (§6.3). canonicalize() deliberately deletes extensions.studio before hashing, so workingTreeHash ignores every image/look/timeline/run; and because Zod strips unknown keys by default, validating a raw studio object as a v1 op silently lobotomizes it.

**Evidence**: canonicalize.ts:52 strips the studio namespace inside any extensions object; workingTreeHash/commitContentHash run through canonicalize (:67-111). The migrator routes all unmodeled richness there: imageGallery is NOT in partitionEntity's known set (migrate-from-studio.ts:88-93); scene runtime fields explicitly stashed (:250-256); commits/branches/storyGraph too (:362-365). Schemas are plain z.object (no .passthrough), Zod is 3.25.30 — so NarrativeSchema/EntitySchema.parse() drops any undeclared …

**Fix**: Keep the finding's (1) and (2), but correct (3): (1) Promote canon-relevant studio content to real modeled fields instead of leaving it to fall into extensions.studio by default-catch-all. Concretely: extend the v1 schema per ratified decision #5 with a `looks[]`/gallery structure on Entity carrying validity anchors (arc/production/commit-range), analogous to how `portraitVariations` is already promoted to `variations: AssetRef[]`. Add `imageGallery` to migrate-from-studio.ts's known-field handling and convert it to that modeled structure so it participates in workingTreeHash. Do a pass over migrate-from-studio.ts's other stashed fields (commits/branches/storyGraph/conversationHistory, scene visualDirty*/lastImage* fields) and confirm each is genuinely ephemeral/runtime before leaving it in extensions.studio — per NIT_FORMAT_SPEC.md §1.9 that namespace is documented as non-normative …

## [CRITICAL] security-1 — Identity/authz sits at T5, but external writes and social posting ship at T3

**Claim**: The plan opens the two highest-risk surfaces — an external `commit_event` write endpoint and automatic outbound social publishing — at T3, while the only mention of an identity/auth model is deferred to T5 (MCP) and T6. This is a build-order inversion: the system will accept authenticated-nobody writes and auto-post to real platforms for two full phases before any identity model exists.

**Evidence**: The live server has ZERO auth: `app.use(cors())` with no options (server.ts:225), no 401/403/authenticate/token check anywhere in server.ts (grep returned nothing), and `app.listen(PORT)` binds all interfaces (server.ts:23184). Any request already reaches any project by supplying `projectId` in the body/query with a global default and no ownership check (server.ts:806, 883, 975...). The docs place identity only at T5: 'per-agent identity/auth' (ROADMAP:67; REVIEW:187), yet T3 introduces …

**Fix**: Don't pull full per-agent identity/auth forward to T3 — that's rightly complex (agent-scoped tokens, MCP identity model) and belongs with T5's MCP exposure. Instead, insert a minimal, narrowly-scoped security gate before T3 ships either of its two external-facing features: 1. Blanket auth on all mutating routes: a single shared-secret/API-key middleware (checked via a header, e.g. `Authorization: Bearer <STUDIO_API_KEY>`) applied to every non-idempotent route, not just the new commit_event endpoint — closing the existing gap (zero auth today) rather than just fencing the new surface. 2. Ownership/project-scoping check: reject any request whose `projectId` isn't associated with the presented credential, instead of trusting a client-supplied projectId (server.ts:806/883/975 pattern). 3. commit_event specifically: require it to land only on a non-canon `ingest/<source>` branch (already the …

## [CRITICAL] security-2 — Untrusted ingested content reaches extraction and character agents with no boundary

**Claim**: The ingest paths (D&D logs, external producer events, social replies) feed attacker-controllable text straight into the extraction LLM and later into character-agents, and the resulting commits then drive auto-render and auto-publish hooks. There is no sanitization, no trust boundary, and no moderation before that content becomes canon and gets projected outward. A crafted session log or fan reply can hijack the extraction agent or a character-agent.

**Evidence**: 'social replies ... ingested as tomorrow's first event' (REVIEW:301); ingest is 'extract → dedupe → proposal commits' with external producers as a source (REVIEW:277-282). No moderation/NSFW/content-filter exists in the server (grep for moderat|nsfw|safety|toxic found only a film-prompt content-filter trick at server.ts:6535, not input screening). The AuthorRef the extractor would stamp is unauthenticated free text — `kind`/`name`/`identifier`, all self-asserted (schemas.ts:75-79), so an …

**Fix**: Treat all ingested text as untrusted: (1) route it through a separate low-privilege extraction context that cannot call tools or emit publish-tagged ops; (2) land ingest strictly on quarantined `ingest/<source>` branches that require human/consistency review before merge to canon (never auto-merge external-origin commits); (3) add an input-moderation pass before extraction.

## [CRITICAL] security-3 — The `publish` tag is an unguarded single point of catastrophic automation

**Claim**: A commit tagged `publish` auto-fires distribution connectors that post to real platforms. The Autonomy Dial only gates PHASE boundaries, not hook firing, so one bad merge, one injected commit, or one character-agent action that reaches canon carrying the publish tag auto-posts with no human release gate, no pre-publish moderation, and no rate limit.

**Evidence**: 'commit tagged publish → distribution connectors ... times the post' and 'Posts are committed back as events' (REVIEW:289, 238-242). Hooks 'filter on tags/ops' and fire automatically (REVIEW:284). The dial pauses at 'phase boundaries' only (REVIEW:236); it is not a per-post release gate. No rate-limit/throttle exists (grep found none). The auto-merge dial explicitly lets 'trusted minor events auto-merge' (REVIEW:270), so a publish-tagged commit could slip through unattended.

**Fix**: Mostly agree with the proposal; refine to fit the plan's own architecture rather than bolt on generic security machinery: (1) Treat `publish` as a privileged tag, not a free-form commit tag — enforce in the T0b operation-emission path (emitOperation()) that only a designated principal (human UI action, or an explicitly publish-authorized agent identity once T5 MCP auth lands) may attach it; strip/reject the tag on commits arriving via T3's external producer API or T6 character-agent paths unless that path is explicitly whitelisted. (2) Make the publish hook itself origin-aware: it must inspect commit provenance (author/principal, ingest branch vs. direct/reviewed branch, auto-merged vs. human-merged) before firing, not just match on tag — this can live in hook-registry.ts's existing dispatch/errorStrategy layer as a precondition check rather than a new subsystem. (3) Add a lightweight …

## [CRITICAL] simplify-1 — Op log can't be the blob's "materialized view" — typed ops cover a fraction of state

**Claim**: T0b routes the executor switch through emitOperation() appending typed GraphOperations "alongside the blob write" and calls the blob a "materialized view" of the op log. But the v1 GraphOperation union is only 17 ops over entities/relationships/scenes/frames/style/scratchpad (schemas.ts:335-385). The ~130 studio tools mutate productions, acts, timeline items, video takes, explorations, assets, artifacts, generatedImages, castLooks, taste memory, dream state, and the 10-rung script ladder — NONE of which have a GraphOperation. So the op log is a strict subset of state; the blob is NOT derivable from it. That is two sources of truth, not one, and every downstream that assumes op-stream …

**Evidence**: schemas.ts:335-385 (GraphOperationSchema = 17 entity/scene/frame/style/scratchpad ops only). server.ts grep for emitOperation|GraphOperation|HookRegistry returns ZERO hits (net-new plumbing). ProjectData carries script/acts/timeline/assets/artifacts/generatedImages/storyGraph/interactions as untyped fields (storage-adapter.ts:196-224). Review doc §4 T0b: "the blob stays the materialized view"; §7: "every action already emits typed operations".

**Fix**: Do NOT thread emitOperation() through the 130-tool switch and REST cores. The studio ALREADY snapshots full state at every commit (server.ts:21300-21308 stores entities+relationships+interactions+storyGraph+themes; checkout restores from it at 21424-21427). Collapse "op log" and "commit" into one: at the commit boundary, DERIVE the typed GraphOperations by diffing consecutive snapshots for the canon-relevant subset (entities/scenes/relationships) — one diff function, not 130 call-site edits. Everything else rides in the snapshot as blob-only state. This answers the brief's own question ("Could the op log BE the commit?") — yes, and it deletes all of T0b's write-seam plumbing.

## [CRITICAL] writepath-1 — emitOperation at executor seam (13616) misses most writes; op-log will diverge from blob

**Claim**: T0b's plan to "route the executor switch (server.ts:13616) through a thin emitOperation() that appends typed GraphOperations" cannot faithfully capture mutations, because the executor is NOT where writes land. Writes land in three places the executor seam never sees: (a) 68 self-fetched REST endpoints the tools call, (b) fire-and-forget background jobs that mutate projectData directly, and (c) REST handlers the UI hits directly, never going through the tool executor at all. An op-log built at 13616 will be permanently, silently inconsistent with the blob for the studio's PRIMARY output (rendered/animated/dream scenes).

**Evidence**: runProductionJob mutates and persists mid-run outside the executor: server.ts:6925 `frame.imageUrl = result.imageUrl` then :6934 `saveProjectData(...)` (background job started at 7113 via `void runProductionJob`, and runDreamFilmJob at 7401). Executor tools delegate via self-fetch (68 sites: 6911, 7451, 15036, 15262) into REST handlers that themselves mutate+save. Those same handlers are hit DIRECTLY by the UI, bypassing the executor: app.post('/api/narrative/interactions') server.ts:881, …

**Fix**: The core proposal (diff at saveProjectData against projectDataCache's previous value) is correct and matches existing plumbing. Two refinements worth adding to the plan: 1. Diffing full ProjectData blobs on every save (140 call sites, some hot-path/high-frequency like per-frame render updates) has a real perf cost — structural diff of potentially large arrays (entities, interactions, generatedImages, assets) on every write. Consider a cheap shape: cache a shallow fingerprint (e.g., array lengths + updatedAt fields) to skip full diffing when nothing top-level changed, or restrict initial op-derivation to a whitelist of fields (entities, relationships, interactions, frames, commits) rather than diffing the entire blob including transient/UI-only fields. 2. Diffing loses "why" — an executor-level emit can attach intent/tool-name/actor metadata to an op; a pure before/after diff at …

## [CRITICAL] writepath-2 — The "lossless" studio→nit migrator silently drops timeline, acts, assets, renders, looks

**Claim**: The migrator header and review doc call it a "lossless studio→nit migrator" that round-trips "byte-for-byte" (doc §1B line 68; migrate-from-studio.ts:5-8). It is not. migrateStudioProjectToV1 carries only entities, relationships, interactions→scenes, styleProfile, scratchpad, and stashes just four top-level fields (commits/branches/storyGraph/conversationHistory) under extensions.studio. Every other top-level ProjectData field is dropped: timeline, acts, assets, generatedImages, artifacts, castLooks/temporal looks, tasteProfile, prompt ledger, dream state. Under T4 (nit becomes source of truth) this becomes PERMANENT loss of exactly the state prior work fought to preserve — including the …

**Evidence**: migrate-from-studio.ts:361-365 is the entire top-level preservation: commits, branches, storyGraph, conversationHistory — nothing else. The ProjectData normalizer lists assets, generatedImages, artifacts, script, storyGraph as first-class (server.ts:259-264) plus timeline/acts (server.ts:250); none except storyGraph reach extensions.studio. hashAssets() is unimplemented (only comments, migrate-from-studio.ts:15-17) and no back-translate exists (grep of src/git/format/v1 finds only the …

**Fix**: The core proposal (sweep unknown top-level keys into extensions.studio the same way partitionEntity does per-entity, add a round-trip test, implement hashAssets + back-translate, gate T4 on both) is correct and should be adopted largely as-is, with one adjustment: prefer an explicit allowlist-of-known-canonical-fields (entities, relationships, interactions, scenes, styleProfile, scratchpad/documents — the fields actually converted) and stash everything else, rather than a blocklist of "fields we happen to remember," so newly added ProjectData fields (as server.ts's own comment warns happens) are preserved automatically. Mirror the exact pattern server.ts:246-251 already uses (spread raw first, then overlay only the fields that were structurally converted) to guarantee no top-level field is silently dropped again. Also downgrade/remove the "lossless"/"byte-for-byte" language in both the …

## [MAJOR] concurrency-1 — Shared cached object + no lock: concurrent writers lost-update on one blob

**Claim**: The plan's central multi-writer scenario (nightly run + ingest + human on one world) has no concurrency control; all writers mutate ONE shared in-memory object with last-writer-wins full-blob persistence, and any handler that snapshots-then-reassigns an array across an await clobbers concurrent writers.

**Evidence**: loadProjectData (server.ts:233-237) returns the SAME cached ProjectData reference from projectDataCache (192) to every caller; 142 load sites / 126 save sites and zero mutex/version/lock in the file. saveProjectData (294-320) unconditionally overwrites cache + disk with whatever object it's handed. A tool handler that awaits a multi-second LLM/render call, then assigns `projectData.interactions = rebuiltArray` (computed from a pre-await snapshot), silently drops scenes another handler appended …

**Fix**: Land, at/before T0b, an explicit contract for the emitOperation() seam rather than a blanket await-holding mutex: 1. Add a monotonic `version` field to ProjectData. `saveProjectData`/emitOperation take an expected version; on mismatch, don't overwrite — re-load current data, replay the operation's *typed* GraphOperation (append/patch, not whole-array reassignment) against the fresh object, and retry save (optimistic concurrency, not a lock held across awaits). 2. Never hold any lock across an `await` for an LLM/render call — that would serialize exactly the nightly-run + ingest + human-edit concurrency the plan is designed to support. Instead codify (and lint/review-gate) the pattern the codebase already uses ad hoc: resolve targets by id/index against the live `projectData` *after* any await, apply single-item or GraphOperation-scoped patches, never capture-then-reassign a whole array …

## [MAJOR] durability-2 — In-memory job maps die on restart; T3/T6 pile more jobs on same substrate

**Claim**: Every async job registry is an in-memory Map that vanishes on the frequent tsx-watch restarts, and the plan builds nightly hooks (T3) and autonomous character-agent runs (T6) — longer-lived than today's jobs — on this exact substrate while only proposing to fix book-import jobs.

**Evidence**: extractionJobs (server.ts:174), videoJobs (6191), productionJobs (6827), exportJobs (7170), dreamFilmJobs (7391) are all `new Map()` with no disk persistence. REVIEW.md:122-124 documents that book jobs 'die on restart, never auto-commit' and a lore import died this way. Fix scope is only 'durable jobs on disk' inside T2 (172-176), but T3 nightly render hooks (285-291) and T6 autonomous runs depend on the same in-memory job machinery that is never generalized to durable/resumable.

**Fix**: Keep the core proposal (durable, restart-safe job store, landed before T3 schedules unattended work) but two refinements: (1) Don't build it as a wholly separate "jobs/ dir" mechanism parallel to T0b's new operation log — instead have job state transitions (queued/running/progress/succeeded/failed) themselves be appended as typed operations (or a sibling event stream keyed by job id) through the same emitOperation() seam T0b is already introducing, so there's one durable append-only substrate instead of two. (2) Scope it as a shared utility (`createJob`/`updateJob`/`completeJob` helpers used by all five Map-based registries, not just book/extraction jobs) with a startup scan that reconciles any job left `running` into `failed:interrupted` — and require T3's hook scheduler and T6's agent-run loop to be written against this helper from day one rather than copy-pasting the existing …

## [MAJOR] hooks-1 — HookRegistry mismatch: 30s timeout kills renders, no loop guard, volatile history

**Claim**: Binding the existing HookRegistry to the commit stream (T3) will (a) time out every real render hook, (b) create infinite hook cascades given 'commits trigger hooks' + 'posts are commits', and (c) re-fire expensive media generation after any restart because execution history is in-memory.

**Evidence**: hook-registry.ts:38 sets globalTimeout 30000ms and executeSingleHook (388-408) Promise.race's the hook against it — but the plan's first hooks are dream_film/compose_comic renders (REVIEW.md:177-180) that take minutes. No recursion/hop-count/provenance guard exists anywhere in the registry, while REVIEW.md:116-118 & 238-242 make hook-generated posts into commits ('hook commits trigger hooks', 'Posts are themselves commits'). executionHistory is an in-memory Map (hook-registry.ts:32,465) that …

**Fix**: Mostly agree with the proposal; sharpen it: 1. Do NOT try to fit media renders into the existing hook interface at all — the interface's synchronous execute()-with-timeout shape is wrong for anything longer than seconds. Instead, a hook's job is only to *enqueue* a durable job (reusing whatever job/queue mechanism T2's "durable jobs on disk" already introduces for session ingest) and return immediately; the actual dream_film/compose_comic render runs out-of-band and writes its own commit when done. Keep Promise.race + globalTimeout only for genuinely fast hooks (portrait-on-entity-add might still be too slow — check). 2. Add commit provenance: every GraphOperation/commit produced by a hook carries `extensions.hooks.originHookId` (using the already-ratified `extensions.<system>.*` metadata convention from §6.4 — this is free, no new schema needed) plus a hop count. The dispatcher refuses …

## [MAJOR] missing-2 — Asset content-hashing (hashAssets) unimplemented — blocks T4

**Claim**: The canonical v1 schema requires content-addressed AssetRef.sha256 and T4 promises 'content-addressed commits', but nothing computes real asset hashes; the 'lossless migrator' emits placeholder hashes, so no phase actually delivers content-addressing or asset dedup over an already-2GB store.

**Evidence**: schemas.ts:49-50 AssetRefSchema requires sha256: Sha256HexSchema; but migrate-from-studio.ts:15-40 emits 'URI_PLACEHOLDER_<id>' / placeholderHash() and its own header says 'A separate hashAssets() pass walks the assets/ directory, computes real sha256s' — that pass does not exist anywhere (grep hashAssets in src/: only the two comment references in migrate-from-studio.ts). No content hashing on write in the studio either — the only 'dedup' is by URL string (server.ts:2767 'if list.some …

**Fix**: Add hashAssets() as an explicit, named task (not just an inventory footnote) and make it a gate before any phase claims content-addressing: 1. Implement hashAssets(): walk the asset store (generated-images/ and wherever studio assets live), compute real sha256 over bytes, rewrite AssetRef.sha256 + cachedUri, and build a content-addressed blob store (hash -> path) so identical bytes collapse to one stored copy — this doubles as the dedup pass the 2GB/792-file store currently lacks (current dedup is URL-string equality only, server.ts:2767/2517). 2. Gate T0b (or whichever phase first writes AssetRefs into the v1 graph) on hashAssets existing for new writes going forward; gate T4 explicitly on a completed migration pass that has replaced all placeholder hashes in already-migrated data — T4 should not claim/ship 'content-addressed commits' while any AssetRef in the graph still carries a …

## [MAJOR] missing-3 — No scheduler for the 'nightly cron' the plan depends on

**Claim**: The plan repeatedly hangs its flagship 'players play by day, canon renders by night' loop on a 'nightly cron hook', but there is no scheduling primitive anywhere and the hook system is exclusively commit/event-typed — a time-based trigger has no home in any phase.

**Evidence**: Roadmap and review invoke 'nightly cron hook' / 'At 2am the nightly hook composes...' (TRANSMEDIA_INTEGRATION_REVIEW.md:286-288, TRANSMEDIA_ROADMAP.md:60-61). But the hook registry types are all event kinds (src/git/hooks/types.ts — ENTITY_ADDED/SCENE_COMPLETED/COMMIT_CREATED etc.), fired on graph operations, not clock time. In the server the only setInterval is a websocket heartbeat (server.ts:18830); grep cron/nightly/schedule in server.ts finds no scheduler. T3 describes hooks as 'on-commit …

**Fix**: Add a small explicit scheduler to T3 rather than folding it into the hook registry: a durable timer/cron source (e.g. a lightweight node-cron or a persisted "next-fire" timestamp checked on an interval, so it survives process restart) that, on firing, either (a) enqueues a pipeline run directly over the day's canon delta (commits since last run), or (b) emits a synthetic COMMIT_CREATED-shaped event so it can still ride the existing hook-dispatch path without expanding HookTriggerType's semantics to include time. Give it the same per-production autonomy dial, budget/QC gate, and run-log as other pipeline triggers (dream_film, compose_comic) so "nightly" is configurable (cadence, time-of-day, per-production on/off) rather than hardcoded to 2am. Name it explicitly as a distinct component in the phase plan (e.g. "T3b — Scheduler") instead of describing it as one of the "on-commit triggers."

## [MAJOR] missing-4 — No search/retrieval or embeddings for a months-long world

**Claim**: T5/T6 make character-agents query 'what that character knows (canon + their scenes)' over a world that accumulates productions/arcs for months, but there is no indexed or semantic retrieval layer anywhere; retrieval is loading the entire project blob and truncating, which neither scales (blobs already 54MB) nor lets an agent find relevant context.

**Evidence**: grep for embedding/vectorSearch/cosine/pinecone/faiss across src/: zero results. Context assembly everywhere is naive truncation (server.ts:1167 description.slice(0,300), 1911, 2093 cast.slice(0,5), etc.). Reads load the full ProjectData blob (projectDataCache, server.ts:296). Roadmap T6 requires per-character knowledge-scoped queries (TRANSMEDIA_ROADMAP.md:69-71); no phase adds an index/embedding/retrieval service to support it.

**Fix**: Add "T4.5/T5-prereq — SCOPED QUERY LAYER" as a named, gating dependency of T5 (not just a nice-to-have for T6). Two refinements to the original proposal: 1. Sequence it right: don't wait for embeddings. T0b already introduces typed `GraphOperation`s with structured fields (entity ids, tags, story-time, branch, arc). That structure makes a cheap inverted/faceted index (by entity name+alias, tag, arc id, branch, story-time range, production) sufficient for T5/T6's stated need ("what canon + which of their scenes") without semantic embeddings at all — MVP should be this index, built incrementally as operations are appended (same emitOperation path from T0b), not a separate batch job over the blob. Treat embeddings as a stretch goal for fuzzy "what does Aria know about X" queries, not a blocker. 2. Give it a storage home explicitly: extend the `NitStorageAdapter` interface (ratified …

## [MAJOR] missing-5 — No retraction/unpublish path for autonomous distribution

**Claim**: The distribution decision auto-posts on commits tagged publish and the usage narrative explicitly worries about 'a posted page later contradicted', yet no phase provides any takedown/retract/unpublish capability — once a distributor agent posts, a subsequent canon change (merge, fork-into-arc) leaves the public post permanently divergent from canon.

**Evidence**: §6.8 (review:238-242) 'commit tagged publish → connector posts... Posts are themselves commits'; the day-in-the-life resolves a conflict by FORKING published content into a glitch-arc (review:298-301) — but the already-posted page is not addressed. grep unpublish/retract/takedown/deletePost/redact in server.ts: zero. Distribution is described only as outbound posting; there is no inverse operation and no phase (T3-T6) mentions retraction.

**Fix**: Add retraction/reconciliation to the T3 distribution connector contract, but scope it minimally rather than a full generic 'retract' verb: (1) every publish-triggered post commit must store the connector's external post id/permalink as extension metadata on that commit (extensions.distribution.<connector>.postId etc.) — this is the load-bearing prerequisite and costs little since posts are already committed back as events; (2) a canon-contradiction hook (fires on merge/bounce/fork-into-arc touching entities/scenes referenced by a published post) enqueues a review-queue item, not an automatic silent delete — autonomous silent takedowns are their own hazard (Streisand effect, fan-facing inconsistency) and should default to human-reviewed per the same Autonomy Dial already governing publish; (3) the connector interface gets an optional `annotate`/`retract` capability per platform (some …

## [MAJOR] missing-6 — No CDN/public asset serving for social distribution

**Claim**: T3 distribution posts rendered comic pages / clips to social platforms, but all generated media lives on local disk served only via res.sendFile from the app process; there is no cloud upload, public URL, or CDN, so there is nothing a social connector can actually attach.

**Evidence**: Assets are served locally: server.ts:2549/6160/7536/7545/7553 res.sendFile(...) from .narrative-data; grep s3/gcs/cloudinary/cdn/putObject/publicUrl/presigned in server.ts: zero. AssetRef only carries cachedUri (canonicalize.ts:53 'local serving hints') and an externalUri hint that 'other tools MUST resolve via sha256' (schemas.ts:60). 2.0GB of images sit under .narrative-data/generated-images. No phase provisions public/CDN serving.

**Fix**: Add a minimal 'asset publish' step to T3, but scope it modestly rather than as full CDN infra: (1) reuse the existing sha256 content-addressing already in AssetRef — publishing just means making the sha256-keyed file reachable over the public internet; (2) simplest viable version: an env-configurable PUBLIC_BASE_URL + reverse-proxy/tunnel (or a single pluggable ObjectStoreAdapter mirroring the NitStorageAdapter pattern, file-first/S3-later) so distribution connectors get a real externalUri to attach; (3) only assets referenced by a commit tagged `publish` get uploaded/pinned — everything else stays local and eligible for the existing disk cleanup, avoiding a wholesale migration of the 2GB generated-images directory. This should land as a dependency of the T3 distribution connectors, not a prerequisite for the rest of the plan.

## [MAJOR] missing-7 — Cost governance is per-job only, not unified across pipelines

**Claim**: Budget caps exist per single run (maxClips / renderBudget) and even require a human confirm above 20 renders, but T3 hooks fire multiple pipelines automatically with no human in the loop (nightly dream_film + compose_comic + distributor agents; T6 many character-agents), and nothing aggregates spend across them — so the world-level nightly/monthly cost is unbounded.

**Evidence**: Budget is scoped to one job: server.ts:7385 maxClips per dream_film (default 20, hard-capped 60 at 7513), server.ts:15284 renderBudget per dream run (cap 40), and the tool description tells the agent to 'Confirm the budget with the creator if >20 renders' (server.ts:11830) — a per-run, human-gated guard. There is no account/world spend ledger (grep spendUsd/costUsd: none). Hooks (T3) run unattended, so the per-run confirm can't fire, and N triggered pipelines each get their own cap with no …

**Fix**: Add a lightweight spend ledger as a T3 prerequisite, implemented as its own GraphOperation type (fits the existing emitOperation/T0b plan cleanly) rather than a bolt-on service: 1. Every paid action (render, animate/chain clip, NB2 page gen, TTS/audio job) emits a `spend` operation {agentId/hookId, jobId, unitsOrUsd, ts} into the same per-project operation log T0b already introduces — no new storage adapter needed. 2. A small `checkWorldBudget(projectId, windowMs)` helper sums spend ops in the rolling window (e.g. trailing 24h) before any autonomous-dial job starts or continues a stage; hook dispatch (T3) and the dream/dream_film job runners call it alongside their existing per-job maxClips/renderBudget checks — the per-job cap stays (cheap early-exit), the world check is the outer guard. 3. World budget cap is a per-project setting (default conservative, e.g. $X/day) surfaced in the …

## [MAJOR] product-3 — Production switcher is a pervasive single-project refactor, not a switcher-above-the-rails

**Claim**: §8's 'Total new UI bill' lists 'production switcher above the rails (T0a)' as one of three small additions. In reality the studio is built on one-production-per-project everywhere. The UI holds single-production state as top-level useState (page.tsx:1409 scenes, 1445 scriptDoc, 1471 acts, 1476 timeline), all hydrated from the single active project. The server reads `.interactions` (scenes) at 220 sites, `.script`/`.timeline` singular, and resolves the project via a global `getActiveProjectId()` at 124 sites. Pluralizing to productions[] means threading a productionId through all of that plus every tool that reads projectData.script/timeline/interactions directly.

**Evidence**: page.tsx:1409/1445/1471/1476 single-production useState; grep counts: `.interactions` 220, `getActiveProjectId` 124, `projectData.timeline` 9; review doc line 158 T0a admits existing script/timeline 'become productions[0]'.

**Fix**: Mostly agree with the proposed shim approach, with one adjustment: rather than a single global `getActiveProduction(projectData)` resolver (which doesn't by itself solve the problem, since the 220 `.interactions` sites still need to be repointed from `projectData.interactions` to `production.interactions`), split T0a explicitly into two sub-milestones and update both docs to reflect the real shape of the work: 1. **T0a-i (data-model + compatibility shim):** Add `ProjectData.productions[]` with existing `script`/`timeline`/`interactions`/`acts` migrated into `productions[0]`, but keep top-level getters (`getActiveScenes(projectData)`, `getActiveScript(...)`, `getActiveTimeline(...)`, `getActiveActs(...)`) that default to `productions[0]` (or the caller's resolved `activeProductionId`) and mechanically replace the 220/124/9 direct-field accesses with calls through them (a scripted …

## [MAJOR] product-5 — Nightly-render review fatigue is hand-waved; no auto-merge policy engine exists

**Claim**: The nightly hook composes comic pages + a microdrama over the day's delta ('players play by day; canon renders by night', §7), and 'a day in the life' shows a daily queue (12 events, 4 pages, 1 clip). The plan's relief valve is 'trusted minor events auto-merge (dial)' — but auto-accept is explicitly disabled today (server.ts:20195-20196: 'All proposals require explicit user confirmation — no auto-accept'), and no trust/confidence policy exists to decide what merges without a human. For one operator, unbounded nightly output plus all-manual review is the exact failure mode the plan generates rather than solves.

**Evidence**: server.ts:20195-20197 forced manual confirmation; server.ts:7384-7450 dream_film maxClips budget is the only volume governor and it caps spend, not review load; §7 relies on unimplemented 'auto-merge (dial)'.

**Fix**: Add a named, scoped subtask under T3 (not just "hooks bind to op stream"): a merge-policy component consisting of (1) a small typed rule store (op-type × tag × actor/source × confidence-band → auto-merge | queue | bounce) persisted alongside NitStorageAdapter, (2) confidence emission at the point ops are produced (extraction/dedupe confidence, ingest-source trust level, contradiction-check result) rather than invented post-hoc, (3) the Canon rail's "v1 listing" upgraded to show auto-merged items in a collapsed/undo-able state (not just merge/bounce actions on a flat queue) with a daily digest cap, and (4) explicit relaxation of the current hard-coded "no auto-accept" invariant at server.ts:20195-20196 gated behind this policy check rather than removed wholesale. This should ship in the same T3 slice as the nightly hook itself (or immediately precede it) since the "day in the life" …

## [MAJOR] product-6 — Fork-into-arc has no data model — arcs don't exist and aren't specified

**Claim**: §7-8 make 'fork into an arc' (conflict->content) a headline Canon-rail action, and §6.7/T0a assert project-level arcs[]. But only `acts[]` exist today (scene grouping within one production, server.ts:12170), and neither doc specifies a ProjectArc schema or what fork-into-arc actually writes — a branch? an arc record? a production? Without that, the button has no target and the interaction is a slogan.

**Evidence**: storage-adapter.ts:216 and server.ts:12164-12205 show only acts[] (scene grouping); ProjectData (storage-adapter.ts:196-224) has no arcs field; review doc lines 156-162 assert arcs[] without a shape; §8 line 326 lists 'fork into an arc' as an action.

**Fix**: Define ProjectArc {id, thesis, entityIds, targetStates, branchName, sourceConflictId, canonProgress} in T0a, and specify fork-into-arc concretely as: create a branch from the conflicting commit + create an arc record linking both sides + deep-link into the story workbench. Ship the schema before the button.

## [MAJOR] schema-4 — Temporal looks have no story-time coordinate; anchors conflate authoring vs story time

**Claim**: Decision §6.5 ('the resolver picks the look by story-time', anchors = arc/production/commit-range) has no sound semantics: none of the three anchors is a story-time axis, they don't total-order across productions, and they break on branches — while replacing a per-scene label mechanism that already works.

**Evidence**: Today looks are labeled entity.imageGallery entries resolved by case-insensitive label match, with per-scene lock scene.castLooks and per-shot entityLooks override (server.ts:11930-11933, resolver 6458-6477, 13417-13457). No story-time field exists; scenes carry only position (SceneSchema :235, migrate :206), which is per-production and mutated by REORDER_SCENES (schemas.ts:363). A commit-range anchor is AUTHORING time: a prequel comic authored after a scar is added resolves to the scarred look …

**Fix**: The core diagnosis and remedy direction are right, but tighten two things: 1. Don't require a fully general branch-scoped `chronologyIndex` before any progress — that's a real feature but bigger than what's needed to unblock T1-T2. Cheaper interim step: keep resolving via the existing working label/castLooks mechanism (already correct and already branch-implicit since it operates on the live working set), and treat "look validity" as an explicit interval on a NEW branch-local integer field on Scene (e.g. `chronologyIndex`, distinct from the authoring `position` used by REORDER_SCENES), defaulting to `position` until authors actively reorder narrative time — so the migration path is non-breaking. 2. Model `entity.looks[]` as first-class canonical schema (not extensions.studio) with `{label, ref, validFrom?: chronologyIndex, validTo?: chronologyIndex}`, and make `castLooks`/`entityLooks` …

## [MAJOR] schema-5 — Typed medium payloads misdiagnose v1's 'gap' and duplicate Scene/Frame

**Claim**: The plan says v1 must add typed medium payloads to 'close g89le's own payload:any gap' (§3, §6.1) — but v1 ops are ALREADY fully typed (no op-level payload:any), and g89le's ScenePayload/FilmPayload are COMMIT-level bags whose contents duplicate v1's Scene/Frame. This is redundant representation, not a gap being closed.

**Evidence**: v1 op payloads are concrete schemas per type (schemas.ts:336-385) — no payload:any at op level. g89le's gap is GraphOperation.payload:any (git-types.ts:29). But g89le's 'typed commit payloads' (DESIGN.md §2.2:114-144) are a SEPARATE commit-level field payload:TypedPayload (:104) alongside operations[], and ScenePayload.{panels,dialogue,visual_beats} (:116-123) restate v1 Frame.{dialogue,visualBeat,participantRefs} (schemas.ts:205-224). The only use g89le makes of medium typing is hook routing …

**Fix**: Drop the "typed medium payload" schema addition (ScenePayload/FilmPayload/SessionPayload/ComicPayload) from T0/T1 scope entirely — v1's per-operation-type payloads (already typed) plus Commit.tags[] (already present) cover both the data-shape and the hook-routing needs g89le used TypedPayload for. Concretely: - For hook/pipeline routing (g89le's `payload_type`), reuse `Commit.tags[]` — e.g. tag commits with `medium:comic` / `medium:film` / `medium:session` — which is already wired into src/git/hooks' `RealityHook.filter.tags`. - For medium-specific structural needs beyond what Frame/Scene already carry (comic page layout/gutters, PDF export settings, panel arrangement not reducible to a Frame), add those fields to the Production container (`ProjectData.productions[].format` + medium-specific config), not to GraphOperation or a new Commit.payload — this matches what T1 actually does in …

## [MAJOR] schema-6 — A 4th Entity/Scene/Commit type system; interactions vs Interaction silently equated

**Claim**: The plan says 'no second graph engine… delete the loser later' (§5), but T0b wires a NEW seam between four incompatible type systems, and the migrator papers over a collision: the studio's scene list is named interactions, g89le has a DISTINCT Interaction (beat) concept, and v1 has no Interaction at all.

**Evidence**: Four Entity shapes: v1 strict EntitySchema (schemas.ts:140); g89le git-types importing types.ts Entity {id,name,type,[key]:any} (types.ts:25-30); g89le NarrativeCommit/GraphOperation payload:any (git-types.ts:25-35); studio ProjectData.entities:any[] (storage-adapter.ts:197) with referenceImage/imageGallery. Studio scenes are physically stored in projectData.interactions (server.ts:950 pushes newInteraction; :864-1083 treat interactions as scenes) and the migrator maps raw.interactions → v1 …

**Fix**: Narrow the fix to what's actually needed: (1) Add a single explicit line to the T0b bullet (or a short glossary note) stating "studio.interactions == v1.Scene; the term 'interaction' is retired as a schema name past the migration boundary — new code reads/writes `scenes`." (2) Add a code comment at migrate-from-studio.ts:354 (`raw.interactions -> scenes`) recording this is a deliberate rename, not a lossy guess. (3) Add a short note to §3/§6 clarifying that g89le's `packages/nit/src/types.ts` and `git-types.ts` (Entity/Scene/Interaction/GraphOperation with payload:any) are explicitly NOT imported — only the Aureum package, the pipeline-phase pattern, and the adapter convention are — so nobody mistakes the beat-level `Interaction`/`ADD_INTERACTION` for something that needs a v1 equivalent. No deletion work needs scheduling since those g89le types were never going to be wired in; the ask …

## [MAJOR] schema-7 — New project arcs[] collides with existing acts[] (already 'arcs') and script.sceneList

**Claim**: T0a adds project-level arcs[] as 'long-arc planning above any one production' (§4 T0a), but ProjectData already has acts[] documented as 'broad sweeping arcs that contain scenes', plus script.sceneList and script.beatSheet — three scene-grouping layers the plan neither reconciles nor renames.

**Evidence**: ProjectAct (storage-adapter.ts:117-133) is described verbatim as 'top-level organizing unit… broad sweeping arcs that contain scenes', linked via scene.actId; ProjectScript also carries beatSheet[] (:95-100) and a stage-9 sceneList[] of 30-40 scene cards with linkedSceneId (:103-111). Adding project.arcs[] plus per-production acts[] yields two things literally called 'arcs' at different scopes over an already-overloaded ladder. v1 has none of these containers (schema-2), so the migrator has no …

**Fix**: Mostly agree with the proposed single spine, with two adjustments: 1. Keep "act" as the per-production structural term (already used pervasively — ProjectAct, scene.actId, Storyboard's Acts→Scenes→Shots UI, and referenced by the timeline/pipeline docs) and use "arc" ONLY for the new project/world-level, cross-production container — i.e. exactly invert which layer gets renamed from what the proposal suggests. Don't rename ProjectAct to "sequence"; that would touch every existing scene.actId reference, the Storyboard UI, and the pipeline-restructure docs for no semantic gain, since "act" already reads correctly as a per-production/per-story-instance concept and never collided with anything before T0a's project.arcs[] showed up. The new world-level thing is the one that needs to pick a non-colliding name if 'arc' is wanted at that scope too — alternatives: `campaigns[]`, `threads[]`, or …

## [MAJOR] security-4 — No identity model is specified; AuthorRef is self-asserted, not server-stamped

**Claim**: 'Per-agent identity' appears as a phrase but is never designed. The canonical schema's authorship is unverified free text, and T4's content-addressing gives content integrity but NOT author authenticity. Without a real principal model the whole 'who committed, on which branch, at what dial' framing (REVIEW:248-249) is unenforceable.

**Evidence**: AuthorRefSchema = `{kind: user|ai|system, name, identifier?}`, all client-suppliable (schemas.ts:75-79). The current commit path hardcodes `author: 'user'` (server.ts:7731). Docs mention identity only as an unspecified phrase (ROADMAP:67; REVIEW:187).

**Fix**: Keep the five-part model but re-phase it against the plan's own timeline rather than bundling it all into T3/T5: 1. **At T0b (now, cheap)**: when `emitOperation()` is introduced, have it derive `AuthorRef` from the server-side request context (session user, or a fixed `{kind:'system', name:'studio'}` for today's single-operator deployment) instead of trusting a client-supplied field. Even a stub principal is fine as long as the codepath that later adds real auth doesn't have to touch every call site — this alone kills the server.ts:7731-style hardcode-by-copy-paste pattern before it spreads to more endpoints. 2. **Before T3 ships (not deferred to T5)**: T3's whole point is admitting external, less-trusted callers (producer API/hooks, the P89 terminal adventure) to the commit path. That's the actual trust boundary crossing, so minimal Principal + credential-checked auth middleware needs …

## [MAJOR] security-5 — No secrets model for the social/distribution connectors T3 introduces

**Claim**: T3 outbound connectors need real platform OAuth tokens/API secrets, but there is no secrets storage or scoping anywhere — the system is a flat unencrypted JSON blob per project and all current config is bare process.env. Credentials for multiple character accounts cannot live safely in this model.

**Evidence**: No oauth/twitter/mastodon/bluesky/webhook/connector/secret-vault code exists (grep across server.ts and src/git returned nothing); the only 'secrets' in the codebase is a narrative lore field (hooks/types.ts:147). Persistence is `.narrative-data/project_<id>.json`, an untyped blob (REVIEW:42-43).

**Fix**: Mostly agree with the proposal; two refinements: (1) Don't scope this only to a "NitStorageAdapter or separate secrets adapter" as an implementation detail to defer — make it a explicit T3 pre-requisite deliverable in the roadmap doc itself (a named sub-phase, e.g. T3a), since T3 cannot ship its stated "connector posts" capability without it, and T6 character-agent accounts depend on the same mechanism (so design it once, not per-phase). (2) Keep secrets fully out of the nit commit/graph-operation stream, not just out of ProjectData: since T0b routes all mutations through emitOperation() and T4 makes commits content-addressed/diffable/mergeable, any credential that ever touches a GraphOperation or commit payload becomes permanently recorded in history (unlike a mutable JSON blob it can't later be scrubbed). Concretely: a separate `SecretsAdapter` …

## [MAJOR] security-6 — Platform ToS, impersonation, rate limits, and pre-publish moderation unaddressed

**Claim**: T6 has agents autonomously running real social accounts and posting AI-generated content, but the plan treats a post as merely 'a commit.' Real platforms enforce automation rate limits, bot-disclosure/impersonation rules, and moderation; auto-publishing unmoderated generated media at scale risks account bans and real-world harm (impersonating real people/orgs).

**Evidence**: 'an agent runs a character's social account ... its actions = commits (posts become events)' (ROADMAP:68-72); '@AriaChen_89 answers a fan reply' (REVIEW:301). No moderation of generated output before publish and no rate limiting exist (grep).

**Fix**: Same core proposal, with two additions: (1) explicitly gate the very first T3 distribution connector (not just T6) behind this policy layer, since REVIEW.md §6 item 8 already schedules real social posting at T3 — waiting until T6 to police it means the ungoverned version ships first; (2) treat the moderation pass and impersonation guard as blocking, synchronous steps in the `publish`-tagged commit's hook pipeline (not an async/best-effort check), and log a rejected-post commit (e.g. `publish/rejected`) back into the graph so blocked content is auditable rather than silently dropped — keeping with the plan's own principle that "posts are committed back as events."

## [MAJOR] simplify-2 — v1 isn't "canonical" and the migrator isn't lossless — drops script/acts/timeline

**Claim**: §6.1 ratifies v1 (src/git/format/v1) as THE canonical schema "where the data lives," and §1 calls migrate-from-studio.ts a "lossless studio->nit migrator." It is not lossless: migrateStudioProjectToV1 reads only entities, relationships, interactions, styleProfile, documents, and stuffs commits/branches/storyGraph/conversationHistory into extensions.studio — it never reads raw.script, raw.acts, raw.timeline, raw.assets, raw.artifacts, or raw.generatedImages, so all of those are dropped on migration. v1 models roughly the entity/scene/frame core (~40% of what ProjectData actually holds); making it "the canonical schema" means the studio's real work becomes second-class extensions.studio.* …

**Evidence**: migrate-from-studio.ts:352-382 reads exactly raw.entities/relationships/interactions/styleProfile/scratchpadDocuments||documents and explicitly preserves only commits/branches/storyGraph/conversationHistory (lines 362-365); no read of script/acts/timeline/assets/artifacts/generatedImages. schemas.ts NarrativeSchema (314-329) has no production/act/timeline/asset-catalog/artifact fields. storage-adapter.ts:196-224 shows those fields exist on ProjectData. schemas.ts:9 self-declares "the running …

**Fix**: Don't demote v1 wholesale — T0b already keeps ProjectData as the materialized view/source of truth for reads and only appends typed GraphOperations alongside the blob write, so the "two permanent models" risk is largely pre-empted. Instead: (1) fix the false claim directly — strike "Lossless"/"lossless" from migrate-from-studio.ts's docstring and from doc line 67; replace with an accurate scope statement (e.g. "migrates the entity/relationship/scene/style/scratchpad core; script, acts, timeline, assets, artifacts, and generatedImages are NOT yet modeled and are dropped, not preserved"). (2) Before or as part of T0a, make an explicit, named decision on the dropped fields: either extend NarrativeSchema with production/act/timeline/asset-catalog/artifact structures so productions[]/arcs[] have a real v1 home (consistent with T0b's "typed medium payloads" extension already planned in §3), …

## [MAJOR] simplify-5 — T4 re-implements snapshot commits/branches/merge that already work in the studio

**Claim**: T4 ("FULL NIT: real branches, merge via the existing NarrativeMergeEngine/paradox resolver") treats commits/branches/merge as missing. They are not: the studio already has snapshot commits, branch tracking, checkout-from-snapshot, and a common-ancestor 3-way merge with conflict detection. Pulling in src/core's NarrativeMergeEngine/paradox-resolver — which operate on the TemporalNarrativeGraph, a different graph type from ProjectData — would be a SECOND merge engine, exactly the "no second graph engine" trap §5 warns against.

**Evidence**: server.ts: commit snapshots at 21300-21308/21620/21697; checkout restore at 21424-21427; branch merge with findCommonAncestor + detectEntityConflicts at 21880-21918. The real gaps are narrow: branch:'main' is hardcoded at only ~5 mutation sites (683,699,712,7730,8497, plus branch update 21342) and merge diffs only entities (21910), not scenes.

**Fix**: Keep the core rescope but correct two things: 1. Branch hardcoding: don't cite the "5 sites" as given — 683/699/712 are inside the unused `demoCommits` fixture array, not live writes, and 21342 already reads `session.currentBranch`. The confirmed real hardcode sites needing `session.currentBranch` threaded through are `/api/canon/import/commit` (server.ts:7730) and the world-generation genesis commit (server.ts:8497); do a grep-driven audit for `branch: 'main'` / `branch:'main'` literals (excluding the demo arrays) rather than trusting a fixed count. 2. Merge: rescope T4 to extend the EXISTING `/api/narrative/merge*` endpoints (findCommonAncestor + detectEntityConflicts, server.ts:21770-22135) to also diff scenes and relationships — but do this by porting the *algorithms* already implemented in `src/core/narrative-versioning.ts`'s `NarrativeMergeEngine` (`detectSceneConflicts`, …

## [MAJOR] writepath-3 — T0a mislocates data: scenes live in interactions[] (203 refs), not script

**Claim**: T0a describes pluralizing production as "existing script/timeline become productions[0]" (doc §4, §8). But scenes — the bulk of a production — do not live in `script` or `timeline`; they live in the top-level `projectData.interactions` array (203 refs), while `script` (6 refs) and `timeline` (17 refs) are separate small fields. Nesting interactions/timeline/acts under productions[] means threading a productionId through 200+ sites, all 68 self-fetch bodies, the background jobs (which write to top-level interactions with no production scoping), and every getActiveProjectId() fallback — which becomes doubly ambiguous (it cannot say WHICH production a write targets). The scope estimate is off …

**Evidence**: `projectData.interactions` appears 203 times vs `script` 6 and `timeline` 17 (grep). Scenes are created into interactions everywhere: server.ts:950, 2236, 3233, 18057, 20604. Background jobs resolve scenes from interactions with no production key: server.ts:6889. projectId is already fragile — getActiveProjectId() fallback at 30+ handlers (806, 883, 975, ...) is documented gotcha #8 (page.tsx:2356-2358, wrong project's data when the id isn't threaded); a productionId dimension multiplies this.

**Fix**: Keep the proposal essentially as stated, with one addition: explicitly enumerate ALL top-level arrays that need the productionId backfill, not just interactions/timeline/acts — grep for other top-level ProjectData arrays created per-scene or per-episode (e.g. any storyboard/panel arrays, exploration/dream-run logs, comic pages once T1 lands) so the migration touches every writer, not just the three named ones. Also explicitly patch the one background job identified at server.ts:6889 (and any sibling job-processing loops that resolve `scene = projectData.interactions.find(...)`) to accept/require a productionId on the job record itself, since jobs are long-running and outlive a single request's productionId context — this is the one site where "add a foreign key on create" isn't sufficient by itself, because the job needs to carry productionId forward across its own async lifecycle, not …

## [MAJOR] writepath-4 — Op-log append + blob write are two non-atomic writes; tsx restart will diverge them

**Claim**: The blob is persisted with a deliberately SYNCHRONOUS fs.writeFileSync (server.ts:298-307) precisely to survive a tsx-watch restart killing the process mid-write. If the op-log is a SEPARATE append (separate file, separate write), the two are no longer atomic: a crash between the blob write and the op-log append leaves them inconsistent — and background jobs call saveProjectData repeatedly mid-run (server.ts:6934 inside a per-shot loop), opening many such windows during exactly the long-running dream/produce jobs most likely to be interrupted.

**Evidence**: saveProjectData does a single synchronous blob write (server.ts:307 fs.writeFileSync) with a comment (298-303) explaining it exists to close the restart-loss window. Any op-log kept as a separate ops_<id>.jsonl append is a second syscall with no shared fsync/rename atomicity. runProductionJob saves once per shot in a loop (6934), so a 20-shot animate run has 20 divergence points.

**Fix**: Adopt the proposal essentially as stated — append operations into ProjectData itself (e.g. `ProjectData.operationLog: GraphOperation[]`) so the existing single synchronous writeFileSync in saveProjectData covers both blob and ops in one write; do not introduce a second independent file/write for the op-log. Two refinements: (1) since operationLog will accumulate forever otherwise, define the flush point explicitly — when ops are batched into a commit (per the 'Commits = batched ops' language in the doc), truncate/rotate operationLog in the SAME saveProjectData call that persists the commit, not as a separate write; (2) note that fs.writeFileSync alone is not fully crash-atomic either (a kill mid-write can still leave a truncated/corrupt JSON file, independent of the op-log question) — if that risk is judged worth closing at the same time, write to a temp file in the same directory and …

## [MINOR] scale-3 — projectDataCache is unbounded; every touched world stays fully resident in RAM

**Claim**: Nothing evicts loaded worlds from the cache, so a server that touches several multi-tens-of-MB worlds (nightly runs iterate projects; T0a makes each world larger) grows resident memory without bound.

**Evidence**: projectDataCache = new Map (server.ts:192); loadProjectData sets and never deletes (266,275,284,296); no size/count cap anywhere. Real worlds are 54MB/28MB/25MB on disk and larger parsed. T0a (REVIEW.md:154-158) grows each world by folding in all productions, and nightly hooks (285-291) load worlds unattended.

**Fix**: The core proposal (bound the cache, evict idle worlds) is right; two refinements: (1) Don't build a bespoke LRU cap purely by count — project sizes vary 100x (12KB to 52MB in this repo), so a naive N-entries LRU can still blow memory if the N resident happen to be the giant ones. Cap by estimated resident bytes (e.g., track JSON.stringify(data).length or a running estimate at cache-set time) with a configurable ceiling, evicting least-recently-used entries by access timestamp until under budget — not just entry count. (2) Sequence this behind T0a/T3 rather than as an urgent fix: it's the T0a productions[] growth and the T3 nightly-hook multi-project traversal that turn this from theoretical into real, so the eviction work belongs in the same phase that ships those two changes (call it out as a T0a/T3 acceptance-criterion line item), and pair it with keeping saveProjectData's synchronous …

## [MINOR] missing-8 — No observability/run-log for unattended pipelines and hooks

**Claim**: The entire model becomes unattended autonomous pipelines and hooks from T3 on, but there is no persistent run log, audit trail, or hook-failure record; the only 'report' is a scratchpad note the agent writes about itself, which is absent when a run crashes or a hook throws.

**Evidence**: grep winston/pino/createLogger/auditLog across src/: zero — logging is console.* only (e.g. server.ts:309, 7450). The hook registry has an in-memory onError callback and a log level (src/git/hooks/types.ts:261,283; hook-registry.ts:431 this.log('error',...)) but nothing persists across restart. The 'morning report' is written by the agent via write_scratchpad_note (server.ts:15289) — self-authored, not emitted on failure. Book/extraction jobs are in-memory Maps (server.ts:174) that die on …

**Fix**: Scope it down from the original proposal, which conflates two different things: (1) in-memory job durability (already ratified and owned by T2 — don't duplicate) and (2) process/execution observability for hooks and autonomous runs (genuinely unaddressed). Refined: at T3 (hooks), extend HookRegistry so execution records (start/finish/duration/error/hookId/commitId) are flushed to a small append-only JSONL/NDJSON run-log file per project via NitStorageAdapter (not the narrative operation log — keep process telemetry separate from narrative content) — the in-memory executionHistory Map can stay as a hot cache, just add a persistence hook on record. Surface failed/crashed runs (a run with a start entry but no matching finish/error after a timeout) in the Canon rail as a distinct 'failed run' item alongside morning reports, so a crashed nightly hook is visible the next morning instead of …

## [MINOR] schema-8 — v1 ops are bare {type,payload}; no id/timestamp/author, so Canon rail can't batch

**Claim**: The Canon rail is specced as a commit inbox — 'incoming branches grouped by source with staleness', 'listing operation-log batches with merge/bounce' (§8) — wanted at T0b/T3, but real commits (author/parentHashes/timestamp) are deferred to T4, and v1 ops carry no metadata to synthesize batches from.

**Evidence**: GraphOperation members are just { type, payload } (schemas.ts:336-385) — no id, timestamp, author, or ordering key, unlike g89le's GraphOperation with id+timestamp+metadata (git-types.ts:25-35). CommitSchema (schemas.ts:393-405) has author/parentHashes/timestamp/branch, but real commits are T4 (§4). So a T0b/T3 'operation-log batches' inbox (§8) has no per-op source, time, or grouping for staleness/source-grouping, and no author to attribute a bounce.

**Fix**: Either add a lightweight envelope to each appended op at T0b (opId, ts, author, batchId/branch) so the log is orderable and attributable without full content-addressing, or pull a minimal commit envelope (author/timestamp/branch/optional parentHash) forward from T4 into T0b so the Canon rail has real batches to list. Do not promise the Canon rail against bare ops.

## [MINOR] simplify-6 — New project-level arcs[] duplicates the existing acts[] concept

**Claim**: T0a adds arcs[] as "long-arc planning above any one production" and §7 calls arcs "intentions ... thesis + entities + target states." But acts[] already exists and is described in-code as "top-level story act (broad arc that groups scenes) ... the master organizing unit." Shipping both leaves four overlapping container concepts (productions, arcs, acts, branches) with two of them both literally called "arcs," which will confuse the model and the UI.

**Evidence**: server.ts:12170 tool desc: "Create a new top-level story act (broad arc that groups scenes). Acts are the master organizing unit"; ACTS endpoints at 3271. Review §4 T0a: "Add arcs[] at project level ... scenes/productions link to arcs."

**Fix**: Keep two containers — they operate at genuinely different scopes (within-production scene grouping vs cross-production campaign intention) — but stop overloading the word "arc" for both. Concretely: (1) rename the new T0a project-level container from `arcs[]` to something distinct, e.g. `threads[]` or `campaigns[]` (the review itself already uses "long-arc planning" and "thesis" language that fits "narrative thread" better than "arc"); (2) leave `acts[]` and its existing `arc` field/tool description alone, but when T0a lands, scope `acts[]` explicitly to a single production (acts group scenes within one script/timeline) so its relationship to the new project-level container is unambiguous by containment, not just by name; (3) update the ACTS tool description at server.ts:12170 to stop calling an act "a broad arc" once the new top-level concept exists, so the agent-facing tool …
